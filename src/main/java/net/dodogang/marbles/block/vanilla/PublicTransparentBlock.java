package net.dodogang.marbles.block.vanilla;

import net.minecraft.block.TransparentBlock;

public class PublicTransparentBlock extends TransparentBlock {
    public PublicTransparentBlock(Settings settings) {
        super(settings);
    }
}
